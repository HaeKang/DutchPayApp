<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.apptest">

    <application
        android:allowBackup="true"
        android:hardwareAccelerated="true"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".View.WithdrawActivity"></activity>
        <activity
            android:name=".View.Duth.AddDuthActivity"
            android:screenOrientation="portrait"
            android:theme="@style/popupTheme"></activity>
        <activity android:name=".View.Duth.SetDuthActivity" />
        <activity
            android:name=".View.Duth.DuthActivity"
            android:label="@string/title_activity_test"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".View.Qrcode.ScanQRActivity"
            android:screenOrientation="fullSensor"
            tools:replace="screenOrientation" />
        <activity android:name=".View.Qrcode.QRcodeActivity" />
        <activity android:name=".View.BalanceActivity" />
        <activity android:name=".View.Main.MainActivity" />
        <activity android:name=".View.Sign.SignUpActivity" />
        <activity android:name=".View.Sign.SignInActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

    <uses-permission android:name="android.permission.INTERNET" />

</manifest>